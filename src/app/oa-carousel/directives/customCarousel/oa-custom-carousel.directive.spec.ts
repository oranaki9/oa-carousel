import { OaCustomCarouselDirective } from './oa-custom-carousel.directive';

describe('OaCustomCarouselDirective', () => {
  it('should create an instance', () => {
    const directive = new OaCustomCarouselDirective();
    expect(directive).toBeTruthy();
  });
});
